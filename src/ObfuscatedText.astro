---
import ObfuscatedData from './ObfuscatedData.astro';
import { Type } from './Type';

interface Props {
  text: string
}

const { text } = Astro.props;
---

<ObfuscatedData data={text} type={Type.TEXT} />
