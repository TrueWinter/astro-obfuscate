---
import ObfuscatedData from './ObfuscatedData.astro';
import { Type } from './Type';

interface Props {
  email: string
  text?: string
}

const { email, text } = Astro.props;
---

<ObfuscatedData data={email} text={text} type={Type.EMAIL} />
