---
import ObfuscatedData, { type StyleableComponent } from './ObfuscatedData.astro';
import { Type } from '../Type';

interface Props extends StyleableComponent {
  phone: string
  text?: string
}

const { phone, text, ...other } = Astro.props;
---

<ObfuscatedData data={phone} text={text} type={Type.PHONE} {...other} />
